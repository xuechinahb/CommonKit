<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ssf.commonkt">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <!-- 声明UI设计稿的尺寸 -->
        <meta-data
            android:name="design_width"
            android:value="750" />
        <meta-data
            android:name="design_height"
            android:value="1334" />

        <activity android:name=".ui.welcome.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.main.MainActivity" />
        <activity android:name=".ui.rv.RvDemoActivity" />
        <activity android:name=".ui.rv.simple.SimpleListAdapterActivity" />
        <activity android:name=".ui.rv.simple.SimpleSupportAdapterActivity" />
        <activity android:name=".ui.rv.simple.SimpleHeaderFooterActivity" />
        <activity android:name=".ui.rv.simple.SimpleMultiAdapterActivity" />
        <activity android:name=".ui.rv.simple.SimpleDelegateAdapterActivity" />
        <activity android:name=".ui.state.StateLayoutActivity" />
        <activity android:name=".ui.lazy.LazyFragmentActivity" />
    </application>

</manifest>